# 評価

ライブリロードを使うと、かなり、flutterと近い状態で開発可能。

コストを抑えたい時は、こちらでいいかも。

# 注意点

- node_modulesを使っているので、セキュリティは若干不安なので、そこは気をつけた方がいいと思う。

- 内部でブラウザが動いているだけなので、パフォーマンスは、WEBアプリケーションと同じみたいなので、動作を軽快にするためではなく、マーケティングのためのアプリと割り切る必要はありそう。


